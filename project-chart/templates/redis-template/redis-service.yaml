apiVersion: v1
kind: Service
metadata:
    name: redis
    namespace: {{ .Values.namespace.name }}
    labels:
        app: redis
spec:
    clusterIP: None
    selector:
        app: redis
    ports:
      - name: redis
        port: {{ .Values.redis.service.port }}
        targetPort: redis
        protocol: TCP
